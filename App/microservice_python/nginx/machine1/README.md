# nginx/machine1

## Fichiers

- `default.conf`: vhost Nginx, proxy `/api/` vers `api-machine-1:5000`.
- `index.html`: page web statique de test de la machine 1.

Acces hote: `http://localhost:8181`.
