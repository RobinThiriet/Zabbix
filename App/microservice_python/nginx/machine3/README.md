# nginx/machine3

## Fichiers

- `default.conf`: vhost Nginx, proxy `/api/` vers `api-machine-3:5000`.
- `index.html`: page web statique de test de la machine 3.

Acces hote: `http://localhost:8083`.
