# nginx/machine2

## Fichiers

- `default.conf`: vhost Nginx, proxy `/api/` vers `api-machine-2:5000`.
- `index.html`: page web statique de test de la machine 2.

Acces hote: `http://localhost:8082`.
